include _auto-generated/_-all
.heading
    h1.heading_content!=echo("esc_html(get_admin_page_title());")
.settings
    .setting__content
        .setting-section.setting-section--vars
            .setting-section__content
                form.setting-form(action="options.php" method="post")
                    :php
                        settings_fields($this->plugin_name);
                        do_settings_sections($this->plugin_name);
                        submit_button();
